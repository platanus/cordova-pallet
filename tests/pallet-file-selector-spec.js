ngDescribe({
  name: 'Async upload directive',
  modules: 'platanus.cordovaPallet',
  inject: [],
  element:
    '<pallet-file-selector></pallet-file-selector>',

  tests: function () {
    it('test', function() {
      expect(true).toEqual(true);
    });
  }
});
